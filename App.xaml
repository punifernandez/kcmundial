<Application x:Class="KCMundial.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:KCMundial"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <local:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        
        <!-- Fuente The Seasons -->
        <FontFamily x:Key="TheSeasonsFont">Segoe UI</FontFamily>
        
        <!-- Estilos modernos y profesionales -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource TheSeasonsFont}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#F5F5F5"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
        </Style>
        
        <!-- Estilo para botones principales -->
        <Style TargetType="Button" x:Key="ModernButtonStyle">
            <Setter Property="FontFamily" Value="{DynamicResource TheSeasonsFont}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Padding" Value="32,16"/>
            <Setter Property="Background" Value="#8c52ff"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" 
                                                  Direction="270" 
                                                  ShadowDepth="4" 
                                                  Opacity="0.25" 
                                                  BlurRadius="12"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#7a3fe6"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#000000" 
                                              Direction="270" 
                                              ShadowDepth="6" 
                                              Opacity="0.3" 
                                              BlurRadius="16"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#3A3A3A"/>
                    <Setter Property="Foreground" Value="#888888"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="ComboBox">
            <Setter Property="FontFamily" Value="{DynamicResource TheSeasonsFont}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="#F5F5F5"/>
            <Setter Property="BorderBrush" Value="#4A4A4A"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="20,14"/>
        </Style>
    </Application.Resources>
</Application>

